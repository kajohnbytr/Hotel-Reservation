# Server
PORT=5000

# MongoDB (required)
MONGO_URI=mongodb://localhost:27017/aurora
# Or MongoDB Atlas: mongodb+srv://USER:PASSWORD@cluster.mongodb.net/aurora

# JWT (required) - use a long random string, e.g. openssl rand -hex 32
JWT_SECRET=your-secret-at-least-32-chars

# CORS - frontend origin (default: http://localhost:5173)
CORS_ORIGIN=http://localhost:5173

# Email (optional, for password reset)
# EMAIL_USER=your-smtp-user
# EMAIL_PASSWORD=your-smtp-password
# If email is not configured or NODE_ENV=development, forgot-password returns OTP in response (otpForDev) so you can test with any email.

# AI services (optional; used by /api/ai/* proxy)
# AI_ML_URL=http://127.0.0.1:5001   # Python ML room recommendation
# AI_NLP_URL=http://127.0.0.1:5002   # Python NLP chatbot
